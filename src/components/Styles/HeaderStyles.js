// material-ui imports
import { styled } from '@mui/material/styles'

export const HeaderLinks = styled(() => {
    return ({
        logoLg: {
            width: '100px',
            cursor: 'pointer'
        },
        desktopIcons: {
            color: '#009688',
        }
    })
})
